name: Deployment
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
    - name: Build 🔧
      run: npm run build
    - name: Deploy to GitHub Pages
      run: npm run deploy
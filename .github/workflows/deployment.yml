name: Deployment
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: Deploy to GitHub Pages
      uses: JamesIves/github-pages-deploy-action@v4.3.4